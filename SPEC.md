# Firstlight 产品规格

状态：MVP 范围已确认，正在按 2026-09-07 修订意见修复与回归验证。

2026-09-07 修订：第 4.5 节按确认意见只压缩、加密 Notes 正文；笔记标题、ID、时间、书签、设置及顶层元数据保持明文。不增加历史格式兼容。

本文是 Firstlight 的唯一产品与数据流规范。实现、测试和部署都必须先能在本文中找到依据；没有写进本文的功能不得自行加入。

## 1. 产品定义

Firstlight 是一个极简的新标签页，提供两个运行端：

- Chrome 扩展：读取并展示 Chrome Bookmarks，监听浏览器书签变化，并负责与 Gist 交互。
- Online 网页版：在 Cloudflare Pages、Vercel、GitHub Pages 等静态托管平台运行，读取 Gist 快照并展示。

产品定位：

> Chrome Bookmarks 是唯一的本地书签真相；Gist 是跨设备传递和备份快照；Online 是远程快照的只读查看器。

## 2. 已确认的范围

### 2.1 平台

第一版只支持：

- Chrome Manifest V3 扩展
- 静态 Online 网页版

第一版不支持 Firefox、Safari、Edge、移动端专用版本，也不保留多平台抽象层。将来需要其他平台时，必须先增加新的平台设计和验收标准。

### 2.2 书签

- Chrome Bookmarks 是书签的唯一数据源。
- Firstlight 只展示 Chrome 的“书签栏”及其内部内容，不展示“其他书签”和“移动书签”。
- 扩展只读取、渲染和监听 Chrome Bookmarks；不在 `chrome.storage` 中维护第二份长期书签副本。
- Chrome 的新增、删除、改名、移动事件会触发扩展重新读取并渲染书签。
- 书签编辑统一交给 Chrome 的书签管理器。
- Firstlight 不提供新增、删除、改名、拖拽排序等书签编辑功能。
- 页面提供“打开 Chrome 书签管理器”入口。

拖拽属于书签编辑：如果拖拽只改变 Firstlight 页面顺序，就会产生第二套真相；如果拖拽修改 Chrome，就已经超出只读展示范围。因此第一版不实现拖拽。

### 2.3 背景

背景只使用本地渲染，不请求任何远程视觉资源：

- 纯色
- 内置静态渐变
- 内置 WebGL 动态渐变；WebGL 不可用时回退为同色的静态 CSS 渐变

第一版不调用 Unsplash、Bonjourr 原作者服务、背景代理或任何第三方图片 API，不下载或缓存远程背景。

### 2.4 Gist

- Gist 保存完整的 Firstlight 远程快照。
- 每个 Firstlight Gist 只有一个状态文件，固定命名为 `firstlight.json`；书签、设置、Notes 等所有文本状态在同一个原子快照中读写。
- 所有可序列化为文本的用户状态都可以进入快照，包括书签、打开行为、背景设置，以及未来的纯文本记事本内容。
- 图片、Blob、Object URL、本地缓存和 token 不进入快照。
- 扩展只有在用户明确保存 token、且自动发现或创建远端快照成功后，才会在 Chrome Bookmarks 或已同步设置发生变化后以 10 秒尾端防抖上传完整快照：每次新改动都重新开始 10 秒计时，只有连续 10 秒没有任何改动才发起一次请求。没有 token 时，任何本地改动都不得显示“上传中”。
- Online 只能读取和展示 Gist 快照，不能上传，也不能修改 Chrome Bookmarks。
- 用户界面只要求输入 GitHub token，不存在“配置 Gist”这一步，也不展示 Gist ID、候选列表或 `TOKEN ONLY` 等内部连接状态。扩展保存 token 后自动查找带有 Firstlight 标识和 `firstlight.json` 的 Secret Gist；不存在则立即用当前快照创建。历史原因出现多个候选时自动绑定 GitHub `updated_at` 最新的一份。绑定后的 Gist ID 仅作为本地实现元数据保存。Online 只查找，不创建。
- 创建 Gist、读取 Gist、上传和下载都使用 GitHub API 直连。
- 不使用原 Bonjourr 的 API、代理或后端。

## 3. 数据所有权

```text
Chrome Bookmarks / 已同步设置
      │
      ├── 扩展实时读取 ──→ Chrome 新标签页
      │
      └── 防抖上传 ──→ Gist 快照 ──→ Online 只读展示
                             │
                             └── 启动时本机与远端不同 ──→ Diff 选择本机或远端
```

数据优先级固定为：

1. Chrome Bookmarks：本地书签的唯一真相。
2. Gist：远程完整快照，不是实时数据库。
3. 扩展本地状态：使用 `chrome.storage.local` 保存 Gist ID、同步基线、最近 3 份恢复点、未完成恢复事务、待上传任务、临时 UI 状态、Notes 和当前生效设置。Notes、待处理 Diff、恢复点独立于连接元数据存储，状态变化不得重复序列化未变的大对象；消息通知只传变化字段。新连接的 token 默认放在 `chrome.storage.session`，只有显式勾选记住才持久保存；旧版已保存 token 保持原来的持久化选择。token、Gist ID、同步基线、恢复点、恢复事务、待上传任务和临时 UI 状态不进入 Gist。扩展不使用 `chrome.storage.sync`，也不保存可替代 Chrome Bookmarks 的长期书签副本。
4. Online 本地状态：token 默认使用标签页 `sessionStorage`，只有用户显式勾选记住才写入 `localStorage`；断开连接清除两处凭证。显示配置和排序偏好保存在本地。首次读取 Gist 时以远端 `config` 为默认显示配置；用户修改后本地显示配置优先，但不得上传或改写 Gist。替换连接一旦开始不得继续显示旧账号的快照；失败时保持明确的未连接/错误状态。

## 4. 同步语义

### 4.1 MVP 同步方式

第一版同时实现自动同步和明确的手动操作：

书签事件唤醒 Worker 时，使用本地保存的书签栏摘要判断该变化是否已处理；初始化读到最新书签不能等同于已安排同步。没有未完成恢复事务时，独立存储的待处理 Diff 在首屏状态返回后读取和校验，完成前禁止同步写入；旧版单文档首次迁移仍需整体读取。

- 自动上传：Chrome 书签或已同步设置变化后，持久化待上传任务并等待最后一次改动后的 10 秒防抖窗口结束，读取最新 Chrome Bookmarks 并上传完整快照；同一连续编辑过程只发起一次远程请求。待上传截止时间必须同时由当前 Service Worker 的定时器和 Chrome Alarm 调度；Worker 中止或重启不得丢失任务，恢复运行后按已保存的截止时间继续。临时网络失败保留任务并采用有上限的指数退避重试，鉴权失败或进入 Diff/恢复流程时取消任务。
- Notes 的创建、删除、标题和正文自动保存同样属于已同步状态变化：输入立即更新当前 Note ID 对应的页面内存状态，再以 300–500ms 尾端防抖保存本地完整 Notes 数组，随后进入统一的 10 秒远端上传防抖。切换、新建、删除或接收远端 Notes 时，旧 Note 的草稿绝不能写入新的选中 Note；Notes 窗口在防抖结束前关闭时必须立即刷新最后输入。10 秒静默期结束前不得请求 GitHub 或显示 `Checking remote…`，上传开始后才允许读取一次远端以确认基线未被其他设备修改；Notes 保存不得切换设置页的全局 busy 状态或使无关按钮闪烁。
- Notes 的异步保存响应可能与后续输入交错；任何先前提交版本的广播或请求响应都不得覆盖提交后继续输入的新内容。临时本地保存失败时应保留待保存版本并自动重试。用户在 Diff 中选择远端前必须取消尚未执行的 Notes 保存和重试；恢复失败才允许恢复本地持久化，恢复成功后编辑器必须强制采用远端 Notes，即使远端内容与上一次父级状态引用相同。 窗口关闭后的重试由页面级保存任务继续承担；整页刷新/离开前立即提交最新草稿，并以仅限扩展端的临时待提交草稿保护未确认输入，成功或明确采用远端后清除，不作为历史版本保存。旧响应识别只保存紧凑版本标识，不保存 20 份完整 Notes 文本。
- 启动检查：已绑定 Gist 时仅在浏览器真正启动或主页首次读取状态后检查远端；Service Worker 因保存 Note、设置或书签事件被唤醒不属于启动，不得触发远程检查。一次启动检查因临时网络错误失败后，下一次主页状态读取允许重新检查；同一次成功检查不得因打开多个新标签页而重复请求。本机与远端语义哈希不同就直接打开 Diff，由用户选择使用本机或远端，不在启动阶段自动覆盖任意一侧。
- 新标签页首屏只等待本地 `chrome.storage.local` 与 `chrome.bookmarks.getTree()`，不得等待 GitHub 请求。读取本地状态后必须立即渲染主页；Gist 启动检查在后台串行执行，完成后再通过状态更新或 Diff 通知已打开页面。远程检查正在进行时，新的主页状态读取也不得排在该网络请求之后。 Notes、设置的本地持久化不得排在 GitHub 网络等待之后；恢复写入期间保留本地互斥，并在网络返回后重新校验本机版本。没有未完成恢复事务时，恢复历史与 Diff 大对象可按需加载。
- 手动上传：`UPLOAD` 是用户明确选择以本机为准，立即用当前完整快照覆盖远端，不额外打开 Diff 或覆盖确认。
- 手动下载：打开本机与远端的单一 diff 视图，用户可直接选择使用远端快照。
- Online：读取 Gist 并展示，不参与写入。

第一版不做字段级合并。若本地和远端都在上次同步后发生变化，必须进入冲突状态，不能自动覆盖任意一侧。

冲突状态以单一 diff 视图展示：固定使用本地打包的 CodeMirror MergeView，以暗色、只读的左右 JSON 形式展示（左侧本机，右侧远端），高亮差异并折叠大段未变化内容，不额外展示“新增/删除/改名/移动”计数。弹窗标题只显示 `SNAPSHOT DIFF`，左右栏使用 `LOCAL CURRENT / GIST REMOTE`；标题区域不得再重复显示 `LOCAL / REMOTE`。
不调用第三方在线 diff API；两份 JSON 不离开 Firstlight，所有对比在当前浏览器内本地生成。diff 视图不提供逐块编辑或字段级合并。远端版本同时提供打开对应 Gist 的链接。用户只能明确选择“用本机覆盖远端”或“用远端覆盖本机”。点击关闭按钮或任一版本选择按钮后，diff 弹窗必须立即从界面消失，不得等待后台网络队列完成后才关闭。 远端 Gist 链接必须直接位于 Diff 对话框中，以当前 Diff 的 Gist ID 生成；不能只依赖底层设置入口。懒加载和生成 Diff 期间同样提供立即关闭按钮及 Escape 关闭，关闭不等待资源或后台任务。

为避免超长 `data:` URL 或其他长字符串阻塞 diff 渲染，diff 使用仅供显示的确定性投影：单个 URL 的 UTF-8 长度超过 4 KiB 时，显示协议、前 256 个字符、完整 UTF-8 字节数和完整内容的 SHA-256，其余部分折叠。语义哈希、上传、下载、恢复和验证始终使用未折叠的完整原文；显示投影不得写回快照。

同步判断始终以上次成功同步的快照为基线 `B`，当前 Chrome 书签与已同步设置为本机状态 `L`，当前 Gist 快照为远端状态 `R`：

- `L = B` 且 `R = B`：两端都没有变化，不执行同步。
- `L ≠ B` 且 `R = B`：仅本机变化，执行防抖自动上传。
- `L = B` 且 `R ≠ B`：仅远端变化，打开 Diff，由用户选择本机或远端。
- `L ≠ B` 且 `R ≠ B`，但 `L = R`：两端最终内容一致，只更新同步基线。
- `L ≠ B` 且 `R ≠ B`，并且 `L ≠ R`：进入冲突状态，不自动覆盖任意一端。

Firstlight MVP 面向同一用户在不同地点交替使用设备的场景，不处理两台设备在同一时刻并发上传。同步操作按先读取远端、再判断、再写入的顺序串行执行，不增加并发锁、条件写入或 revision 恢复协议。 本版不增加多标签页同时编辑的协作、字段合并或锁机制，重点保障单页连续编辑、关闭、刷新及失败重试。

本机、远端与基线的内容相等判断以完整快照稳定序列化后的 SHA-256 语义内容哈希为准，不比较 Chrome 书签 ID，也不比较 `updatedAt`。哈希必须使用完整、未截断的标题和 URL；`updatedAt` 不进入内容哈希。同步基线同时记录上次成功同步后的本机内容哈希、远端内容哈希和 GitHub `updated_at`。

相同 GitHub 时间戳不得绕过三向哈希判断。旧格式基线无法证明远端未变时要求用户复核。USE LOCAL 与 USE REMOTE 都必须在实际写入前重新核对已审阅的内容；检查与写入之间不承诺跨设备原子条件更新。

首次绑定还没有同步基线时：

- 未找到 Firstlight Gist：用户明确保存 token 时立即创建当前快照；普通启动检查不创建。首次手动上传同样允许发现或创建。Online 始终只查找。
- 找到 Gist，且本机与远端内容相同：直接建立同步基线，不执行覆盖。
- 找到 Gist，但本机与远端内容不同：进入同一个左右 diff 视图，由用户选择“用本机覆盖远端”或“用远端覆盖本机”；首次绑定绝不自动覆盖任意一端。

启动检查只读：只要本机与远端的语义内容不同，就显示 Diff，由用户选择，不在启动检查中自动覆盖任意一端。正常本地编辑产生的持久化待上传任务不属于启动检查；它恢复执行时仍按三向基线判断，仅本机变化才自动上传，远端变化则停止并显示 Diff。用户手动上传/恢复成功后在左下角显示对应非阻塞提示（如“已上传到远端”/“已恢复到远端快照”）。失败时显示可读错误提示，但不弹阻断式模态窗口。若浏览器中止 GitHub Fetch，幂等的 `GET` 和 `PATCH` 请求自动重试一次；仍失败时错误文字只能说明请求被 Chrome 中断，不得暗示用户主动打断。创建 Gist 的 `POST` 不得盲目重发：Firstlight 必须先重新发现 Gist，并按快照语义哈希确认第一次请求是否已经成功；找到相同快照时直接采用，仍无法确认时报告结果未定并等待用户重试，避免产生重复 Gist。

### 4.2 上传

上传前必须：

1. 重新调用 `chrome.bookmarks.getTree()`，不能使用设置页旧数据。
2. 对快照做稳定序列化和内容哈希。
3. 获取 GitHub `updated_at` 和完整远程快照，通过服务器时间判断远端是否发生过更新，并通过 SHA-256 语义内容哈希判断有效内容是否相对上次同步基线发生变化。本次读取已确认 Secret 时，同一上传操作复用该检查，仍须保留写入后的独立回读。
4. 如果远程已变化且与本机形成冲突，停止自动上传并进入单一 diff 冲突视图，不弹出覆盖确认。用户主动点击 `UPLOAD` 或在 Diff 中选择 `USE LOCAL` 时属于明确的本机优先操作，直接覆盖远端。
5. 上传成功后，重新读取远端并验证语义内容哈希与本机一致，再记录远程内容哈希、本地内容哈希和用于展示的服务器时间。

每个快照包含 `schemaVersion`、客户端生成的 `updatedAt` 和完整配置。GitHub API 返回的服务器时间 `updated_at` 用于判断远端是否发生过更新，也用于展示、诊断和历史追踪；完整快照的 SHA-256 语义内容哈希用于判断有效内容是否真的不同。若 `updated_at` 已变化但语义内容哈希仍等于基线，只更新远端时间基线，不进入冲突。客户端生成的 `updatedAt` 只用于展示和诊断，不参与语义内容哈希。

网络错误、权限错误、API 部分失败或本地状态写入失败时，不得更新“已同步”基线。

### 4.3 下载/恢复

下载和恢复必须：

1. 校验远程 JSON 的结构、大小、数量和 URL 协议。
2. 在写入前读取并保存当前 Chrome Bookmarks 快照作为本地恢复点。
3. 手动下载时只显示本机与远端的单一左右 diff，不显示书签、文件夹、新增或删除数量摘要。
4. 用户在 diff 视图中选择“用远端覆盖本机”后直接执行恢复，不再弹出第二个摘要或确认窗口。
5. 启动检查不自动恢复；本机与远端不同必须显示 Diff。恢复前快照只用于写入失败时的内部自动回滚，不提供用户可见的 Recovery 入口。
6. 按远程快照写入 Chrome Bookmarks 和已同步设置。
7. 写入完成后重新读取 Chrome Bookmarks，验证结果与远程快照一致。
8. 验证成功后，将同步基线与恢复事务的清除一起提交。最终提交失败也必须回滚书签、设置和 Notes，不能提前删除恢复日志。

恢复开始前必须把恢复点与目标哈希作为未完成恢复事务原子写入本地状态，并取消待上传任务。恢复过程中发生部分写入、写入失败或验证失败时，必须立即尝试用恢复点自动回滚；只有回滚并重新验证成功后，才可声明本机已恢复到操作前状态。Service Worker 在恢复过程中中止或重启时，初始化必须先根据未完成事务自动回滚并验证，回滚完成前不得启动上传。自动回滚也失败时，停止后续同步并显示明确的恢复失败状态，保留原始恢复点与事务供后续再次尝试。 同步基线和恢复事务的清除必须在本地持久提交成功后才成为有效内存状态；异常恢复点与事务原文必须保留，不过滤后覆盖原始资料。

本地恢复快照仅作为恢复写入失败时的内部自动回滚保险，不上报到 Gist，也不在设置抽屉中提供 Recovery 或历史记录界面。面向用户的版本选择统一由本机与远端的单一 Diff 完成。

### 4.4 书签匹配

- Chrome 书签 ID 是浏览器本地实现细节，不进入跨设备身份判断、内容哈希或恢复验证；恢复后使用 Chrome 重新分配的 ID。
- 远程快照保留父子关系、文件夹标题、书签标题、URL 和顺序信息。
- Chrome Bookmarks 返回的书签 URL 必须作为不透明文本原样进入快照和恢复流程，包括但不限于 `https:`、`http:`、`javascript:`、`data:`、`file:`、`chrome:` 和自定义协议；不得因协议未知而删除、改写或截断。
- `firstlight.json` 为固定 JSON 结构的快照文件：顶层只包含 `schemaVersion`、`settingsVersion`、东八区 `updatedAt`、`config`、`bookmarks` 和 `notes`。`config` 统一包含 `openTarget`、`background`、`dynamicEffectProfiles`、`foreground`、`layout`、`clockPosition` 与 `features`；`layout` 固定包含 `rows`、`columns` 与 `bookmarkAlignment`。`notes` 为笔记数组，每条固定包含 `id`、`name`、`content`、`createtime` 和 `updatetime`。全部时间字段必须使用带毫秒的 ISO 8601 `+08:00` 偏移格式，不使用 `Z`，并验证实际日历日期，拒绝不存在的日期。
- 当前快照 Schema 为 2，设置版本为 1。`schemaVersion` 与 `settingsVersion` 必须和当前版本完全一致，低于或高于当前值都拒绝，不提供跨版本升级、降级或兼容读取。
- 快照字段不仅结构固定，序列化顺序也固定：顶层为 `schemaVersion → settingsVersion → updatedAt → config → bookmarks → notes`；`config` 为 `openTarget → background → dynamicEffectProfiles → foreground → layout → clockPosition → features`；Notes、各子配置和书签节点也按定义顺序重建。读取、写入、上传、下载、哈希和 Diff 展示都必须经过同一个 canonical 快照函数，不得使用设置对象当时的 JavaScript 插入顺序。纯字段顺序变化不得产生 Diff 噪音。
- 同名文件夹必须允许同时存在。
- 重复 URL 必须允许同时存在。
- URL 和单独的文件夹标题都不能作为唯一键；比较的是包含重复项和顺序的完整书签栏树内容。
- 恢复内部可以采用差异修改或清空书签栏后重建，MVP 不固定具体策略。无论采用哪种策略，完成后都必须重新读取并验证完整书签栏树与目标快照的语义内容一致，验证时忽略 Chrome ID；失败时按恢复点回滚。

### 4.5 Gist Notes 压缩与 token 加密

#### 4.5.1 目标与使用方式

- 只压缩、加密 Notes 正文，减少主要文本负载，同时让仅拿到 Gist 链接的人无法直接读取正文。笔记标题、ID、创建/更新时间保持明文，方便用户在 Gist 中辨识上传的笔记；书签、设置及顶层元数据也不压缩、不加密。
- 用户仍然只输入现有的 Gist token，不增加独立密码、恢复密钥、密钥导入导出或加密设置页面。
- 设备 A 使用某个 token 加密上传，设备 B 或 Online 使用完全相同的 token 即可自动解密。解密不得依赖设备 A 的本地缓存、设备标识或其他未随文件传递的参数。
- 同一 GitHub 账号生成的不同 token 不等价；跨设备读取必须使用加密该文件时的同一个 token。
- 新格式的上传默认执行压缩和加密。扩展与 Online 共用同一套编解码逻辑，Online 继续只读。
- 首版保持完整快照同步，书签、设置、全部 Notes 仍在一个 `firstlight.json` 中保存。文本分块、差异上传和字段级合并不属于本次改动。

#### 4.5.2 压缩与加密流程

```text
上传：按 Notes 顺序取正文字符串数组 → 紧凑 JSON → UTF-8 → gzip → token 派生密钥加密 → Base64 → 与明文笔记元数据合成 notes 字段 → Gist
读取：Gist → notes 字段 Base64 解码 → 同一个 token 派生密钥解密 → gzip 解压 → 还原 Notes 数组 → 校验完整快照
```

- 首版使用浏览器原生 `CompressionStream("gzip")` 和 `DecompressionStream("gzip")`，符合现有最低 Chrome 120 的范围，不引入额外压缩库。更高压缩率的算法另行根据浏览器兼容性、插件体积和实际耗时评估。[兼容性依据](https://github.com/mdn/browser-compat-data/blob/main/api/CompressionStream.json)
- 压缩只改变存储表示，必须无损保留 Notes 正文、换行、Unicode、书签 URL 和所有快照字段；不能为了缩小体积截断或改写用户内容。
- 使用现有规则规范化后的 token 作为秘密输入，通过 HKDF-SHA-256 自动派生 256 位 AES-GCM 密钥。派生上下文固定为 `firstlight/notes-envelope/v1`，不包含设备 ID 或 Gist ID。用户无需单独保存派生密钥。[HKDF 说明](https://developer.mozilla.org/en-US/docs/Web/API/HkdfParams)
- 全部正文一起压缩、加密，避免逐条 gzip 和派生密钥的体积/耗时开销。每次新加密生成 32 字节随机盐和 12 字节随机 IV，AES-GCM 使用 128 位认证标签；盐和 IV 随密文保存，token 和派生密钥不进入文件。格式、算法、盐、IV、明文笔记元数据列表作为认证附加数据参与校验，编码顺序固定，避免正文被替换到另一条标题下。[AES-GCM 参数说明](https://developer.mozilla.org/en-US/docs/Web/API/AesGcmParams)
- 先压缩再加密。Base64 只用于把二进制密文放进 Gist 的文本内容，不能当作保密措施；其约三分之一的体积开销必须纳入压缩效果统计。
- 不承诺所有笔记都能显著缩小，也不承诺上传耗时按体积同比下降。已压缩内容或随机文本可能几乎没有收益。

#### 4.5.3 远端文件与现有同步逻辑

- 文件名仍为 `firstlight.json`，Gist 发现标识、顶层字段和顺序不变。仅远端 `notes` 字段由数组改为以下加密封装对象；解密后恢复第 4.4 节的业务结构，业务 Schema 不升级。本地快照与 Diff 中 `notes` 始终是数组。
- `notes` 封装结构如下；`version` 是 Notes 封装版本，与顶层 `schemaVersion` 独立：

```json
{
  "format": "firstlight.notes.encrypted",
  "version": 1,
  "compression": "gzip",
  "kdf": "HKDF-SHA-256",
  "cipher": "AES-256-GCM",
  "salt": "<Base64 随机盐>",
  "iv": "<Base64 随机 IV>",
  "items": [
    {
      "id": "note-id",
      "name": "可直接检查的标题",
      "createtime": "2026-09-07T10:00:00.000+08:00",
      "updatetime": "2026-09-07T10:00:00.000+08:00"
    }
  ],
  "data": "<正文字符串数组的 Base64 密文，包含认证标签>"
}
```

- `notes.items` 按原笔记顺序保存明文元数据；解密后正文数组必须与其逐条对应、数量相同且每项是字符串。封装不保存明文正文或业务内容哈希。远端文件采用紧凑 JSON；`bookmarks`、`config` 和顶层版本、时间保持原字段值。AAD 为固定字段顺序 `format → version → compression → kdf → cipher → salt → iv → items` 的紧凑 JSON，items 顺序及字段顺序如上；不包含明文书签、设置，不宣称其受到加密保护。
- 内容哈希、同步基线、冲突判断和上传后的独立回读校验继续使用解密后的完整业务快照。相同内容重复加密得到不同密文，不得因此产生内容冲突。
- Diff 和用户主动下载的完整原文继续使用解密后的快照；Notes 本地编辑、保存与恢复流程继续处理原始内容。
- 先验证外层格式、版本、字段和长度，再解密、解压和验证内部 Schema。未知封装版本、错误 token、损坏密文或认证失败必须明确报错，不能降级成明文读取，也不能把失败结果当成空快照。
- 网络读取、Base64 解码和解压都必须有字节上限。首版保留现有 10 MiB 业务快照上限，同时要求最终远端文件不超过现有 10 MiB 传输上限；解压流超过上限立即停止。错误信息区分业务数据超限与编码后的文件超限，不截断、不静默丢弃内容。
- 临时网络失败沿用现有重试规则；解密或格式错误不作为网络错误循环重试。连接已切换或读取已取消时，旧解密结果不得写入当前状态。

#### 4.5.4 格式边界与 token 更换

- 不增加历史配置、旧明文 Notes 或旧加密封装的兼容与迁移分支，仅支持本节当前远端格式及第 4.4 节当前业务 Schema。遇到旧格式明确拒绝，不当成空数据覆盖；本地 Notes 原文保持不变。
- 原 Gist 的历史版本可能仍有明文；覆盖当前文件不能清除历史。本次不自动删除 Gist 或历史记录，也不声称旧版本已经获得加密保护。
- 启用新格式写入前，应先让 Online 和各使用设备具备新格式读取能力。旧客户端无法读取新封装，不支持通过自动写回明文来兼容旧版。
- 保存相同 token 或仅调整“记住 token”时，不重新加密远端。
- 更换为不同 token 时，先验证新连接，保留迁移所需的旧凭证，不能先覆盖旧 token 再尝试解密。若新 token 已能读取目标文件，按正常连接流程处理。
- 支持同账号 token 更换：通过新 token 的用户信息与目标 Gist 的所有者确认可迁移本人 Gist，用新 token 请求远端、旧 token 解密 Notes，再用新 token 加密上传。迁移保留远端原有业务内容，不自动用本机内容替换远端；独立回读校验和本地提交成功后才完成连接切换。
- 迁移写入前须记录可恢复的临时状态，并遵循各凭证的会话或持久化选择；失败或 Worker 重启后暂停同步，用户再次保存替换 token 继续迁移，先尝试读取已用新 token 加密的结果，避免重复写入。网络失败、存储失败不得丢掉完成迁移所需的凭证，也不得提前标记成功。会话凭证因浏览器会话结束而丢失时，要求重新输入相应 token，不尝试覆盖无法解密的数据。
- 旧 token 过期或被撤销，不影响其字符串用于本地解密；GitHub 请求需要有效的新 token。旧 token 丢失且没有其他解密材料时，新 token 无法解开旧密文，必须保留本地数据并停止迁移。
- 切换到其他 GitHub 账号按正常新连接处理，不迁移或改写旧账号 Gist。Online 不执行 token 更换引起的远端重加密。
- 更换 token 只重加密当前快照，旧加密历史仍需要当时的 token 才能解密。

#### 4.5.5 安全边界与验收

- 本方案仅保护 Gist 中的 Notes 正文，笔记标题、ID、时间、书签和设置仍为明文；不能防止 token 泄露或本机凭证被读取。GitHub API 鉴权本身会把 token 发送给 GitHub，因此不宣称能向 GitHub 隐藏内容，也不宣称本地存储已加密。
- 帮助和隐私说明需同步更新：跨设备使用同一个 token；Online 虽只发起读取请求，也必须使用加密时的 token，不能另建不同的只读 token 来解密。
- 必须验证独立客户端仅凭同一个 token 和远端文件完成解密，覆盖扩展上传后另一扩展及 Online 读取；不能只测试同一实例内的加解密往返。
- 必须覆盖中文、Emoji、多行与单行大笔记，确认正文逐字一致；错误 token、密文篡改、头部篡改、未知版本、截断和解压超限均不得覆盖本地数据或推进同步基线。
- 必须覆盖旧格式明确拒绝、标题可直接检查、元数据与正文错配拒绝、上传后回读、创建请求结果不确定时的重新发现、token 更换失败恢复，以及 Online 始终不写 Gist。
- 性能验收使用约 1.2 MiB 的代表性文本笔记，同时加入难压缩样本；记录旧请求体、新请求体、编码和解码耗时，以及真实浏览器中的同步耗时，不能只报告 gzip 输出大小。已有本地模拟文本样本显示请求体可减少约 74%，该结果不是用户实际笔记的压缩率或浏览器性能承诺。

## 5. Online 版规则

- Online 没有 Chrome Bookmarks API，因此不能声称自己“同步 Chrome 书签”。它只展示 Gist 中最近一次快照。
- Online 不得把空书签列表上传回 Gist。
- Online 的只读边界只限制远端数据写入、Chrome 书签操作和 Notes 编辑；背景、前景、主题、Clock、Search、Home Grid、Bookmark alignment 与 Open target 等显示配置必须可以在 Online 设置界面调整并仅保存于当前浏览器本地。
- Online 找不到对应 Gist 时安静显示空状态；不创建 Gist，不显示要求初始化的提示。
- Online 不提供书签编辑、拖拽、排序或恢复操作。
- Online 不缓存 GitHub API 的授权响应。
- Online 的 token 只保存在当前浏览器本地；帮助、错误和诊断信息必须脱敏。
- Online 首版不使用自定义 Service Worker；如果未来需要离线缓存，只允许缓存同源静态 GET 资源。

## 6. 安全边界

- 扩展权限只申请 `alarms`、`bookmarks`、`storage`、`unlimitedStorage`，以及 `api.github.com` 和 `gist.githubusercontent.com` 所需的最小 host permission。`unlimitedStorage` 用于保存 Notes、连接元数据、当前生效设置、待处理 Diff 和最近 3 份本地恢复点，不得用于维护第二份长期书签副本。最低 Chrome 120，与扩展和 Online 构建目标一致。
- 不把书签 URL 发送给 Bonjourr 原作者服务或其他代理。
- 远程 JSON 必须经过运行时 schema 校验，包含 GitHub 平台决定的 10 MiB 最大 UTF-8 字节数，以及最大文件夹数、最大书签数、最大嵌套深度、单个标题长度和单个 URL 长度。Firstlight 不设置低于平台实际传输能力的人为总字节上限；协议也不是快照保存与恢复的过滤条件。
- 所有书签标题和 URL 都作为不透明文本处理，diff 和页面渲染必须使用 `textContent` 或等价的安全 DOM API，任何书签内容都不得被当作 HTML、CSS 或脚本解析。书签能否由 Firstlight 直接打开按协议另行限制，不影响其原样保存和恢复。
- `https:`、`http:` 和 `data:` 书签由 Firstlight 在用户点击后交给浏览器打开。扩展版使用 `chrome.tabs` 发起浏览器级 Data URL 导航，必须保留 Data URL 的原生顶层渲染；Online 因浏览器禁止网页发起顶层 `data:` 导航，使用一次性沙箱启动器把内容转换为 opaque-origin Blob 后顶层打开，不得把实际页面长期嵌套在查看器 iframe 中。`javascript:` 书签只保存、展示和恢复，Firstlight 扩展与 Online 都不得直接执行其中的脚本。`file:`、`chrome:` 和自定义协议同样原样保存、展示和恢复，但 Firstlight 不直接打开。
- Gist token 输入框使用普通明文文本类型，不把用户正在输入的 token 渲染成密码圆点；点击保存后输入框保留并持续显示当前已保存 token，刷新新标签页或重新打开设置后也必须从本地存储回填，不得只显示空框或占位文字。保存前去除复制时附带的首尾引号与空白，并拒绝会导致 HTTP Header 非法的非 ASCII 字符，错误信息必须直接说明 token 内容不合法。
- Online 构建增加 CSP，并且所有远程数据都使用 `textContent` 和安全 DOM API 渲染。

## 7. MVP 页面

### 新标签页

- 书签主内容区位于整个视口的视觉正中心，优先缩短用户移动鼠标到常用入口的距离。
- 中心区域必须在不同屏幕尺寸下保持平衡和美观，不能因为居中而挤压或遮挡内容。
- 显示数字时间；不显示模拟时钟和欢迎语。
- 数字时钟可在设置中选择隐藏、左对齐、居中或右对齐。时钟的对齐基准必须是根层可见内容从最左文字到最右文字形成的实际范围，不是固定宽度舞台；范围过窄时字号应自动收缩，不能越出根层表格。
- 根层书签与文件夹统一使用紧凑、无边框的表格式网格展示，不使用独立方形卡片或大面积留白。
- 点击文件夹后，在对应表格项下方展开该文件夹的直接内容列表；浮层顶部必须紧贴文件夹按钮的底边，垂直间隙固定为 **0 CSS px**，不得添加 margin、位移或 `+ 4px` 等额外偏移。这里的底边是整个可点击表格项的底边，不是文字基线；嵌套文件夹继续在列表中逐层展开，不把所有层级扁平化。
- 点击当前已展开文件夹及其列表之外的任意位置，必须立即收起该文件夹列表；再次点击同一个文件夹也应收起。
- 展开列表不重复显示已经点击过的文件夹名称或项目数量；普通书签项前不添加圆点等无语义装饰。列表纵向间距保持紧凑。
- 用户可在设置中控制首页表格的基础行数与列数；列数决定每行项目数，行数决定至少预留的行数。该配置不是数量上限，根层项目超过基础 `行 × 列` 容量时必须继续向下生成新行，绝不分页、截断或隐藏。
- 增加列数优先横向扩大表格；空间不足时按设置章节的 80% 视口宽度、210px/170px 列宽规则自适应，不产生横向越界。文件夹浮层继承实际列宽。
- 当完整表格高度超过视口时，主页整体允许自然纵向滚动；不得为了保持强制居中而裁掉顶部或底部内容。
- 行数和列数使用 Firstlight 自绘选择器或直接输入，不使用浏览器原生 `select` 下拉框。
- 当前层只展示当前文件夹的直接书签和直接子文件夹；子文件夹不会与父文件夹的内容跨层并列展示。
- 根层文件夹与根层书签使用完全一致的表格单元尺寸、排版和交互反馈，仅用极少量符号区分类型。
- 文件夹可显示其内部递归 URL 数量，但数量文字必须显著小于名称、降低视觉权重，且与名称组成居中的紧凑内容组。
- 根层单个 URL 可使用箭头与文件夹区分，但箭头必须比名称更小且透明度更低，不得抢夺名称的视觉焦点。
- 根层单元格的 hover 不使用突兀的块状或渐变背景；文字反馈只作用于实际可见的“名称 + 辅助标记”内容组，亮绿色细线则横跨当前单个表格列的完整宽度。
- 展开列表应锚定对应文件夹表格项，并根据视口空间自动避免超出左右边缘。仅在下方空间不足且上方空间更充足时允许向上展开，此时浮层底部同样紧贴按钮顶边，间隙为 0 CSS px；不得为贴边而取消视口内限高和内部滚动。主页滚动、视口尺寸或字体大小变化后仍须保持贴边，普通网格与虚拟列表遵循同一规则。
- 文件夹展开列表宽度与所属根层表格单元一致；列表高度不得超出当前视口。内容较多时允许滚轮、触控和键盘滚动，但不显示可见滚动条。
- 文件夹列表背景必须由当前主页背景计算得到，不使用固定纯黑、纯白或固定品牌色。纯色背景下，列表使用同色相的轻微提亮或压暗表面；渐变背景下，必须根据列表在视口中的实际中心位置计算其下方的局部背景色，再基于该局部色生成稳定的轻微分层色面。列表本身不得直接继承全局渐变、渐变两端或渐变角度。列表高亮和顶部提示线跟随当前前景色，使浮层与主页始终属于同一视觉系统。
- 点击是扩展和 Online 两端都必须支持的正式展开方式。
- 在支持精确指针悬浮的桌面设备上，可以使用短延迟的悬浮预览作为增强，但不能依赖悬浮完成导航。
- 触控和键盘操作不依赖悬浮；点击、焦点和 Escape 关闭级联菜单的语义必须保持一致。
- Chrome 书签栏及其文件夹浏览
- 书签搜索默认启用，宽度为主页内容区的大约一半，可在 `Search` 中选择隐藏、左对齐、居中或右对齐。`Search icon` 的 `SHOW / HIDE` 控制放大镜；`Search text` 的 `HIDE / LEFT / CENTER / RIGHT` 控制空输入框提示文字及文字/结果对齐，HIDE 只隐藏提示，结果对齐回退到 Search 位置。隐藏图标或提示都不得关闭输入、搜索和结果能力。搜索覆盖全部层级；结果保持名称为主的紧凑展示，完整文件夹路径通过悬停提示与可访问名称提供，不恢复旧版双列 PATH / NAME 表头。最多展示前 80 个匹配，超过时明确提示缩小查询；无结果时只显示 NO RESULTS。结果浮层使用与文件夹列表相同的局部取色和 hover，可滚动但隐藏滚动条。点击输入框及面板之外立即收起结果，保留查询；再次聚焦时重新展开。
- 搜索框左、中、右对齐的计算边界不是无边框表格的外框，而是当前页最左侧可见文字到最右侧可见文字的实际范围，避免表格空白造成视觉偏移。
- 纯色/渐变背景
- 打开 Chrome 书签管理器
- 同步状态入口：所有用户可感知的错误与同步结果统一通过左下角非阻塞 Toast 显示，包括网络、Chrome 消息通道、本地存储、Notes、设置和组件错误；不得只把错误放在设置抽屉中。后台已通知的同一错误不重复弹出；详细错误仍可在设置中查看。背景不支持等已经成功静态降级的情况不作为错误弹出。
- “仅保存在本机”“已同步”等稳定状态不常驻新标签页；页面只在上传、恢复、发现远端、冲突、失败以及操作刚完成时短暂显示状态提示。详细连接状态只在设置抽屉中展示。

### 设置

- 设置固定在新标签页右下角入口：桌面端 hover 即显示齿轮，点击弹出后右侧抽屉；移动端点击齿轮进入同一抽屉。设置不放在书签级联菜单内，不干扰书签导航。
- 第一次安装后，设置抽屉只在用户第一次打开 Firstlight 新标签页时自动展开一次；同一 Service Worker 生命周期内继续打开新标签页不得重复自动展开。
- 设置抽屉使用紧凑信息密度；顶部品牌名 FIRSTLIGHT 大于栏目标题，不显示 SETTINGS。桌面继续点击外部或按 Escape 关闭；视口不超过 600px 或粗指针设备提供显式关闭按钮，避免全宽抽屉无法退出。栏目标题通过字号和对比度区分，普通操作不重复品牌图标。
- 书签打开位置：首次默认在新标签页打开，用户可在设置中改为当前标签页打开。
- Gist token
- GitHub token 按产品决策在设置中明文显示，并保存在应用/浏览器状态中；无需遮罩，也不得在该决策未改变时将明文显示重复报告为缺陷。
- `SYNC` 栏目内，Token 输入与 SAVE、Remember token 选项、凭证保存及隐私提示组成一个紧凑分组，组内纵向间距统一为 4px，提示紧随选项，不得把保存方式与 Token 输入区拉开。
- 当前同步状态放在 `SYNC` 标题同一行的右侧，使用弱化文字、右对齐；长状态不得挤掉标题或撑出抽屉，可省略显示并通过悬停查看完整内容。不再在 Token 提示下重复显示独立状态行，左下角 Toast 行为保持不变。`CONNECTION HELP & PRIVACY` 链接放在设置内容最下方靠右，位于 `UPLOAD / DIFF / OPEN` 操作区之后；Online 同样放在远端 Gist 入口下方，不夹在 Token 分组内部。
- 自动发现或创建 Firstlight Gist
- 上传快照
- 下载并恢复快照
- `Chrome import` 只重新读取当前 Chrome 书签栏、刷新页面并进入正常的 10 秒上传防抖；它不是远程发现操作，不得显示或遗留 `Checking remote…`、`Importing…` 等无出口状态。没有有效同步连接时只完成本地刷新并显示短暂成功 Toast。
- 背景模式：纯色、静态渐变或动态渐变。
- 纯色模式：选择一个颜色。
- 渐变模式：选择两个颜色和渐变方向，提供即时预览。颜色过渡使用感知均匀的插值方式，并添加极轻的抖动纹理减少深色背景的可见色阶，不得出现明显 banding。
- 动态渐变模式：使用与普通渐变相同的两个原色、Angle 与 `Speed` 配置，但由一张位于主页内容下方的本地 WebGL Canvas 渲染。fragment shader 使用连续的多层噪声和 domain warping 生成相互穿插的流体颜色场，不使用可辨认的椭圆、固定轨道、离散随机跳点、固定角落亮斑、纯色边条或额外白色光斑。随机数只在页面载入时生成一次种子，随后画面由连续时间函数演化；Angle 旋转颜色场的主方向。两端颜色在 OKLab 中插值，并对混合场做平滑的对比度展开，使红蓝、黑白等高对比组合仍能看到明确的两端原色，而不是长期停留在中间色。额外提供 `Speed` 滑杆，有效范围 10–20，默认 10；10 是肉眼可见的最慢档，20 保留明显、夸张的流动，中间使用非线性时间映射。低于 10 的无效刻度不得出现在界面或当前快照中。渲染分辨率设上限，动态背景共用最高 60 FPS 的内部帧率上限（已有低功耗 30 FPS 档保留），不新增帧率设置控件，页面不可见时停止绘制；WebGL 初始化或上下文恢复失败时安静回退为使用同一组颜色与 Angle 的静态 CSS 渐变，不显示错误弹窗，也不影响书签操作。
- 动态背景调参区统一使用直白中文，包括动效颜色、方向、速度、数值参数、布尔参数及其 `aria-label`；动效专有名称（如 `FLOW`、`GALAXY`）可以保留原名。参数标题必须描述用户能观察到的结果，不直接暴露 `Focal`、`Frequency`、`Repulsion` 等 shader 或图形学术语；仅凭标题仍不容易预判效果的可见参数，必须紧邻显示一句简短中文说明。布尔参数统一显示“开 / 关”。
- 第三方动效只保留产品实际需要的控件，不要求完整暴露上游参数。DOT 保留现有颜色、位移强度、圆点大小、圆点间距和鼠标光圈控件，其余六项高级参数保持内部默认/现有配置，不新增入口；PILLAR 保留颜色、流动速度、旋转、宽度和高度，不扩充上游控制。SNOW、FLOW II、FLASH 保留目前已提供的独立控件。新增或删减可见控件时同步更新本规格，不因上游增加参数而自动扩充界面；内部英文数据键保持稳定。
- `SNOW` 采用 React Bits Pixel Snow shader，界面名称与内部效果标识统一为 `SNOW / snow`，并以 Background Studio 链接确认的状态作为 Firstlight 默认值：雪花颜色 `#ffffff`、`flakeSize=0.019`、`minFlakeSize=2.75`、`pixelResolution=500`、`speed=1.35`、`depthFade=10`、`farPlane=15`、`brightness=3`、`gamma=1`、`density=0.5`、`variant=snowflake`、`direction=90`。颜色、速度及其余十项参数必须全部可独立调整。WebGL2 不可用时安静保留黑色静态背景，不阻断主页功能。
- 自研 `Flow` 与 React Bits `Silk` 在 Firstlight 界面分别命名为 `FLOW I` 和 `FLOW II`，固定排在动态效果列表第一、第二位；动效名称保持英文，参数标题与解释继续使用中文。`FLOW II` 以 Background Studio 链接确认的 `speed=9`、`scale=2`、`color=#48b676`、`noiseIntensity=3` 作为默认状态，同时保留上游 `rotation=0` 参数；颜色、速度、纹理大小、颗粒强度和旋转角度必须全部可独立调整。WebGL 不可用时回退为同色静态背景。
- `FLASH` 采用 React Bits Splash Cursor 流体模拟，界面名称与内部效果标识统一为 `FLASH / flash`。以 Background Studio 链接确认的 `CURL=16`、`SPLAT_RADIUS=0.65`、`VELOCITY_DISSIPATION=2.5` 作为默认状态，其余公开参数保持上游默认值：`SIM_RESOLUTION=128`、`DYE_RESOLUTION=1440`、`DENSITY_DISSIPATION=3.5`、`PRESSURE=0.1`、`SPLAT_FORCE=6000`。这些已提供的数值控件及换色速度保持独立调整，不以固定参数总数作验收条件；新增或删减时同步更新本节；效果保持上游彩虹换色与透明叠加方式，不额外显示无效颜色控件。另提供默认关闭的“自动游走”开关：开启后，无真实指针输入时从画面中心或最后一个真实指针位置开始，通过现有指针坐标更新链路模拟平滑、连续、横向扫过画面主要区域且有边缘回正的曲线轨迹；首次从中心向右出发，后续在左右两侧间连续往返。真实鼠标或触摸输入必须立即接管，停止输入约 0.9 秒后自动轨迹从当前位置平滑续接。自动游走不得派发伪造的 DOM 输入事件，也不得绕过用户设置直接注入另一套颜色、力度或流体参数。WebGL 不可用时安静保留黑色静态背景；页面不可见时暂停模拟，组件卸载或参数变化时必须清理动画帧和全局指针监听。
- 内置 Background 模式和动效及其默认配色即本版背景主题入口；用户可调整颜色，不新增独立的静态渐变预设面板。
- 前景显示：在背景配置下方提供主页文字颜色和书签文字大小控制；文字大小允许范围为 12–24px，缺失或范围外的值恢复当前默认值。
- 时间只显示时、分，并提供一个 `Clock seconds` 的 `SHOW / HIDE` 开关控制是否追加秒；不提供日期显示。
- Angle 与 Text size 使用统一的自绘 Range 轨道和滑块，不显示浏览器原生蓝色 focus 光圈。轨道必须具有明确的控件厚度，不能看起来像页面分隔线；滑块使用直角矩形，不使用与整体方块化 UI 冲突的圆形。拖动过程在本地即时预览，并以短防抖静默保存；不得切换全局同步 busy 状态或导致 `UPLOAD / DIFF` 按钮闪烁。
- 左下角同步状态 Toast 与文件夹列表、搜索结果面板使用同一套局部背景取色算法，根据 Toast 所在位置计算色面；不得使用固定黑色弹窗，也不得直接复制主页渐变及其角度。
- 上传、恢复或一致性检查成功后，左下角普通 Toast 必须明确显示完成结果并保留 5 秒；错误与冲突提示保留 10 秒。Toast 到期后只收起通知文字和背景，不改变详细同步状态，也不得在刷新后重新展示已经过期的旧消息。
- Dynamic 背景的实时 shader 色场无法用静态坐标准确预测，因此文件夹列表、搜索结果面板和同步 Toast 不得继续使用静态线性渐变采样值。Dynamic 模式下这些浮层必须改用具有轻微明暗覆盖的半透明局部磨砂面，通过 `backdrop-filter` 直接混合浮层背后此刻的真实像素；底下是红色时浮层保持红色系，底下是蓝色时保持蓝色系。普通纯色和静态渐变继续使用确定性的局部采样色面。
- 首页布局：在前景配置下方提供根层书签表格的行数和列数控制。
- 首页布局提供 `Bookmark alignment` 的 `LEFT / CENTER / RIGHT` 配置，统一控制根层单元及文件夹展开列表的内容组对齐；嵌套层级线和缩进继续表达从属关系，极深层级缩进最多按 12 层展示。它不影响 Clock、Search 或搜索结果的独立对齐。默认值为 CENTER。
- `Theme color` 色块保留在 `FOREGROUND` 中。它是全产品唯一主题强调色，并进入 `config.features.themeColor`，影响书签 Highlight、Notes 标记/边框/分割线、Notes 与 Settings 图标、设置子模块标记和滚动条。正文应保持可读，不直接依赖浅亮强调色承载文字。Range 使用不受主题色影响的中性配色，浅色主题必须有可辨的轨道底衬。
- `themeColor` 是快照唯一支持的主题强调色字段；缺少或取值无效时恢复当前默认主题色。
- 设置是可重建数据。读取本地或远端设置时统一执行白名单投影：合法的当前字段保留，缺失或非法字段恢复默认值，未知字段直接删除，结果在下次持久化时写回。任何旧字段名、旧枚举值或旧效果标识都不得建立专门的别名表、迁移函数或兼容分支，必须和其他未知配置遵循同一条投影规则。设置变化判断必须使用忽略对象键顺序的稳定语义比较；纯键顺序变化不得被报告为损坏、不得清除同步基线，也不得显示修复提示。单个设置字段非法时只修复该字段并显示字段修复提示；只有设置根对象无法读取时才报告已重置为安全默认值。设置字段错误不得阻止上传、下载或恢复；书签和 Notes 仍然严格校验，内容损坏或版本不匹配不得被强制覆盖。本地持久化 Notes 校验失败时初始化必须停止并显示错误，原始存储值保持不变，不得过滤非法条目、写回空数组或继续上传。
- 首页提供一个 `Bookmark details` 的 `SHOW / HIDE` 开关，同时控制所有文件夹后的 URL 数量和主页单个 URL 后的弱化箭头；不得把两者拆成两个开关。
- 增加列数时优先保持 210px 单列宽度，但整个表格宽度不得超过视口的 80%。列间距保持现有紧凑的 1px；空间不足时弹性压缩单列，单列基础下限为 170px，仍然放不下则减少有效列数并继续向下排布。不分页，不产生横向越界。Rows 始终是至少预留的行数，不是容量上限。
- 前景颜色与文字大小作用于主页内容，不影响设置抽屉；数字时钟保留独立的大号比例，但颜色和比例跟随主页前景配置。设置抽屉与 Notes 使用 DARK / LIGHT 对应的高对比文字和中性色面，不固定黑底，不跟随主页背景和前景颜色。
- `FOREGROUND` 设置提供 `Theme` 的 `DARK / LIGHT` segmented control。它统一控制设置抽屉、Notes 窗口和左下角状态入口等应用界面表面的明暗配色，不覆盖用户单独配置的主页背景与前景颜色；默认值为 `DARK`，并进入快照 `config.features.themeMode`。
- 渐变配置只显示两个颜色和方向；在没有清晰名称与用途说明前不显示额外的无标签色块。
- 页面和设置抽屉中的非输入文字默认不可选择；文本输入框仍允许选择和编辑内容。
- 文件夹与书签按钮在鼠标点击、空格激活或 Tab 聚焦后不得显示浏览器原生蓝色填充、白色描边或 focus ring；激活状态继续使用 Firstlight 自身的底部亮色线条。
- 右下角设置齿轮默认完全透明，只在鼠标进入其固定热区或键盘聚焦时显示。设置抽屉关闭时不保留离屏抽屉节点；打开设置时，抽屉外点击区域保持透明，不向主页铺设黑色遮罩。
- 设置抽屉允许滚轮和触控滚动，但不显示浏览器原生滚动条。
- 除动态背景调参区按上述规则统一使用中文外，其他运行时界面、状态、错误和操作文字统一使用英文，不混入中文。
- 同类布尔设置统一使用与 Open Target 相同的 segmented control，不为搜索开关单独设计另一种 toggle 风格。
- 设置抽屉建立唯一的控件长度单位：一个标准按钮为 64px。单按钮操作使用 1 单位；所有下拉框和两个选项的 segmented control 使用 2 单位加统一外框，共 132px；三个选项的 segmented control 使用 3 单位加统一外框，共 196px。下拉菜单展开后必须与触发器同宽，不得出现第四种任意宽度；若选项文字确实放不下，菜单改为同宽的单列垂直列表。Sync 的 `SAVE` 与 Chrome `IMPORT` 使用 1 单位；`UPLOAD / DIFF / OPEN` 操作区暂时保持独立布局。
- 根层书签和文件夹不显示人为添加的序号；文件夹数量只显示数字，不附加无语义的斜杠。
- 自绘选择器必须使用清晰可辨的下拉箭头和足够大的点击区域；搜索入口的放大镜必须在紧凑布局中仍能一眼识别。
- Clock、Search、Rows、Columns 等所有自绘下拉选择器，点击当前选择器之外的任意位置或按下 `Escape` 时必须立即收起；打开另一个选择器时，之前展开的选择器必须自动关闭。
- 设置抽屉关闭时必须重置所有临时展开状态；再次打开时，自绘下拉菜单一律处于关闭状态。

设置面板必须能从新标签页进入，并且关闭后不影响书签级联菜单的当前导航状态。

### Notes

- 左下角提供一个使用项目现有 `lucide-react` `Notebook` 图标的 Note 入口，不使用 Emoji 或手绘字符。无通知时图标和容器背景均完全透明，只在固定热区 hover、键盘聚焦或 Notes 已打开时显示图标；有同步通知时同一个视觉容器显示图标和 Toast 背景，并依据消息长度向右展开，设置最大宽度和文本省略。页面上不得同时出现两个相互错位的左下角元素。
- Note 点击逻辑与同步 Toast 状态在组件职责上保持独立；视觉上共享同一个左下角容器、同一高度、同一基线和同一局部背景计算方式，通知不得改变 Note 按钮的点击语义。
- 点击 Note 图标后在当前主页上方打开居中的悬浮模态窗口，不进入新页面；透明遮罩拦截窗口外的主页交互，点击遮罩只关闭 Notes，按 `Escape` 同样关闭。
- Notes 使用 DARK / LIGHT 对应的中性色面与可读文字、主题色弱强调、直角、细线、方块化视觉，不固定黑底或绿色，不使用 macOS 红绿灯、圆形操作按钮、大圆角或右下角拖拽角标。Notes 打开时是具有焦点约束的真正模态窗口：遮罩必须拦截指针与滚动，不允许点击或操作背后的主页与设置入口；点击遮罩只关闭 Notes，不得同时触发遮罩下方控件。
- 左侧依次包含 Notes 标题、排序控件、实时搜索和紧凑笔记列表。排序行位于 Notes 标题下方，左侧 key 为 `SORT BY`，右侧 value 为 `MODIFIED / CREATED` 两个互斥选项，直接点击切换。必须复用设置面板的 segmented 开关样式及统一尺寸：每格使用同一个 `--control-unit`（当前 64 CSS px），双格控件宽度为 `--control-two`（当前 132 CSS px），不得拉伸铺满侧栏，不使用下拉菜单。标签与两个选项在同一行内，选项文字完整显示、不换行；整行不得越过 Sidebar 左右边界。切换排序时，按钮和 Sidebar 容器尺寸不得变化；DARK / LIGHT、扩展 / Online、窄屏布局均遵守此规则。列表不显示 Note 总数。每条笔记右侧在 hover 或键盘聚焦时显示 `lucide-react` 删除按钮。右侧采用纵向弹性布局：标题栏保持紧凑，正文编辑器自动占满标题分割线以下、底部状态栏以上的全部剩余空间，不使用固定减法高度提前截断。
- Notes 侧栏的排序开关整体右边缘、搜索下划线右端、新增笔记方块右边缘必须落在同一条垂直线上，统一使用 `--notes-panel-inset`（当前 8 CSS px）作为距侧栏内侧右边界的间距，不得分别设置独立的 12px、14px 或 18px 右侧留白。桌面、窄屏与 DARK / LIGHT 使用同一规则；新增或调整侧栏控件时按整列检查对齐，不能只处理单个控件。
- 右侧底部状态栏必须完整保留行数、字符数、文本大小、最后修改时间和创建时间。状态栏固定在编辑区底部，不随正文滚动；空间不足时允许紧凑换行并按内容增加高度，正文相应让出空间。不得用固定单行高度、隐藏、截断或省略号让任何元数据消失；修改 / 创建时间各自保持完整。元数据视觉上弱化但必须可读，字号不小于 10 CSS px，并适配 DARK / LIGHT。调整左侧标题、排序或搜索布局时，必须同时回归右侧元数据在普通桌面、较窄窗口和短视口下的实际可见性。
- 新建按钮使用 `lucide-react` 的 Plus 图标并保持方块化。按钮距列表右边界的间距，与右侧编辑框距内容区右边界的间距共用同一尺寸（当前 8 CSS px）；常规桌面左右并排布局中，按钮底边与编辑框底边对齐，元数据换行导致编辑框高度变化时也保持对齐，不再独立使用 18px 偏移。无笔记时同样对齐空编辑框；窄屏时按钮移至标题行右侧，保留 8px 右侧间距，避免挤压搜索与列表。桌面列表末尾预留按钮所需空间，避免遮住最后一项。窄窗口底部为固定 Notes 入口和 Toast 留出空间；较矮的横向窗口改为左右布局，更小的窗口允许整体滚动以访问完整编辑区。创建后立即选中并聚焦新笔记。标题和正文输入立即更新当前选中 ID 的内存对象，完整 Notes 数组采用 300–500ms 尾端防抖持久化。选中项变化不得依赖异步 Effect 把旧草稿搬运到新对象。
- Notes 与书签、设置一起进入同一个 `firstlight.json` 原子快照并参与语义哈希、Diff、上传和远程恢复；每条笔记的 `createtime` 和 `updatetime` 均为东八区时间。
- Notes 窗口关闭时必须立即刷新尚在 300–500ms 编辑防抖中的草稿，不能因组件卸载丢失最后输入。远端恢复失败时，内部回滚必须同时恢复操作前的书签、设置和 Notes。
- Online 只读展示远端 Notes，可搜索、排序和切换查看，但不能创建、删除、修改或把远端 Notes 副本写入本地持久化存储。
- Online 的显示设置防抖保存必须在 pagehide 时补写。即使离线重连失败，也必须允许清除已保存凭证；断开操作不依赖连接成功。Notes 排序偏好保存失败及恢复事务异常通过左下角 Toast 提示。

不包含：壁纸服务、拖拽编辑、天气、字体服务、多个同步供应商、跨浏览器兼容、原作者 API。

## 8. 实现约束

- 先完成本文件，再创建源码目录。
- 任何新增功能先修改本文件，再实现代码和测试。
- Chrome API、Gist API、快照模型和 UI 状态必须分层，不能互相直接修改内部状态。
- 所有破坏性操作必须有可验证的恢复路径。
- 任何“为了以后可能支持某平台”的抽象，都需要先有实际需求和对应验收测试。
- Chrome 工具栏 Action 只负责打开一个由 Firstlight 接管的新标签页；Manifest 声明 Action 时，构建校验必须确认后台存在对应点击处理器，不得留下无响应的工具栏入口。
- 项目使用 Git 管理源码、规格、测试、锁文件和构建脚本；`node_modules`、`dist`、覆盖率和本地日志不得提交。封版前统一质量门必须执行 ESLint、Prettier 格式检查、类型检查、带最低阈值的覆盖率测试、Chrome API mock 集成测试、扩展与 Online 构建、真实 Chromium 扩展/Online 冒烟测试和生产依赖安全审计；CI 对 push 与 pull request 执行同一质量门。
- UI 验收以正常桌面窗口为范围，例如 1280×800 和 1024×768；不测试手机尺寸或极短视口，也不以这些场景扩展适配需求。

## 9. MVP 验收标准

- Chrome 书签栏变化能在不刷新页面的情况下反映到新标签页。
- 重复 URL、同名文件夹、空文件夹可以上传并完整恢复。
- Gist 下载失败、Chrome API 失败和存储失败都不会被标记为同步成功。
- 恢复写入失败时可由内部恢复点回滚到操作前快照，不提供用户可见的撤销/恢复点入口。
- Online 可以展示 Gist 快照，但永远不会写入或清空远程书签。
- 首版不依赖 Bonjourr 原作者的任何 API、服务或远程资源。
- Notes 回归测试必须覆盖编辑后切换、新建、删除、远端内容刷新、窗口关闭时刷新最后输入，以及开发模式 StrictMode Effect 探测不会写入空 Notes。
- UI 浏览器回归必须实际测量文件夹按钮与浮层边界，确认向下 / 向上展开均无额外间隙（仅允许不超过 0.5 CSS px 的布局取整误差），并覆盖滚动与虚拟网格；Notes 排序需验证标题下 `SORT BY` 标签与双格开关同行、统一格宽、文字不换行及切换后尺寸稳定，不能只检查控件存在。

## 10. 已确认决策

### 渲染与发布验收补充（2026-09-06）

- 超过 500 项的根层列表、展开后的文件夹行与 Notes 列表按可见区渲染；完整滚动范围和最后一项必须可达，键盘 End/Home 和边界 Tab 不得因未挂载的行丢失目标。小列表保留原有布局。
- 搜索索引仅在书签内容变化时重建；目录 URL 计数缓存，不随背景或时钟刷新递归重算。
- 单侧 Diff 文本超过 250,000 字符或项目超过 2,500 时显示摘要、哈希与完整原文下载。普通 Diff 对超长 Notes 同样使用带哈希的显示投影。上传、恢复、校验和下载永远使用完整原文。
- 可选动态背景懒加载，失败保留静态底色；Notes、Diff 与应用根分别有错误边界。静止 DotGrid 不继续刷新；Flash 的普通参数热更新，只有模拟/染料分辨率或 context 恢复才重建 WebGL。
- 自绘选择器支持方向键、Home/End、键入定位、选择后焦点返回与 Escape 分层关闭。真正的模态对话框打开时，背景节点不可交互。不额外绘制键盘 focus ring、亮色描边或改变输入文字颜色；保留输入光标、选择状态、键盘焦点管理和原有 hover/激活反馈。
- Data Viewer 通过独立同源静态启动页取得独立 CSP；启动页必须处于 opaque-origin sandbox，转换后顶层打开 Blob。Firstlight 主页面不允许 iframe 嵌入。
- 所有发布变更使用 `npm run check`，CI 同样执行；源码、配置和 E2E 都受类型/格式/零警告门禁约束。覆盖率报告明确区分单元测试与真实浏览器 GPU/CSP 验证，不把单元覆盖率当作完整用户流程覆盖率。
- 用户可在设置查看 Token 保存方式、数据用途、连接帮助与隐私政策。公开部署使用产物安全响应头，商店后台隐私披露和政策 URL 由正式发布流程填写。

### 10.1 Gist 快照保存什么

已确认：快照保存 Chrome Bookmarks、书签打开位置、主题、背景颜色/渐变，以及未来加入的纯文本记事本内容。不保存 token、浏览器状态、缓存、远程元数据或调试信息。

快照使用 UTF-8 JSON 文本。Firstlight 不设置 900 KiB 或其他低于平台边界的人为上传预算；上传时必须发送完整快照，绝不截断或静默丢弃书签。GitHub REST API 对单个 Gist 文件内联返回的内容上限是 1 MB；返回 `truncated: true` 时，Firstlight 必须通过 `raw_url` 获取完整内容。由于 GitHub 官方要求超过 10 MiB 的 Gist 文件通过 Git clone 获取，而扩展和静态 Online 无法直接执行 Git clone，Firstlight MVP 支持的单个 `firstlight.json` 最大为 10 MiB（按 UTF-8 字节计算）。超过时在上传前显示明确的平台限制错误，不创建、不更新 Gist，也不截断快照。

### 10.2 Online 如何读取 Secret Gist

已确认：Online 让用户在本地输入 token，直接从浏览器请求 GitHub API；Online 只读取，不创建/更新 Gist。token 不发送到 Firstlight 自有服务器，也不能放进 URL、Gist 或日志。
扩展端允许在首次需要上传时自动发现或创建 Firstlight Gist。

### 10.3 是否保留拖拽

MVP 不做拖拽。若决定加入，必须明确拖拽直接调用 Chrome Bookmarks API 修改真实书签，并同步调整“Firstlight 只读展示”的产品定义、权限说明、确认交互和测试范围。
